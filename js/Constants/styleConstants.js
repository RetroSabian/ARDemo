import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
    ProductName: {
        color: '#FFFFFF',
        flex: 1,
        textAlignVertical: 'center',
        textAlign: 'center',
        fontWeight: 'bold'
    }
});